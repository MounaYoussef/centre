{% extends 'base.html.twig' %}

{% block title %}Pro{% endblock %}

{% block body %}
    <h1>Pro</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pro.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ pro.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ pro.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pro_index') }}">back to list</a>

    <a href="{{ path('pro_edit', {'id': pro.id}) }}">edit</a>

    {{ include('pro/_delete_form.html.twig') }}
{% endblock %}
